{
    type: service-conf
    server: {
        type: server-config
        port: 8081
    }
    database: {
        type: database-config
        driver : "org.postgresql.Driver"
        url : "jdbc:postgresql:medisyncdb"
        user : "postgres"
        password : "password"
        thread-pool-size : 32
    }
}